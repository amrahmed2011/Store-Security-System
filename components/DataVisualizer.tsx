
import React from 'react';

// This component is intentionally empty as the app has been simplified 
// to only show motion status as requested.
const DataVisualizer: React.FC = () => {
  return null;
};

export default DataVisualizer;
